#include <iostream>

using namespace std;

void solve() {
    int n,m,k;
    cin>>n>>m>>k;
}

int main() {
#ifdef LOCAL
    freopen("src/codeforce/Round1013(Div3)/in/D.in", "r", stdin);
#endif
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int work = 1;
    cin>>work;
    while (work--) {
        solve();
    }
#ifdef LOCAL
    fclose(stdin);
#endif
}